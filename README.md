# WASM Tester

## Usage

Execute the `run.sh` script to build the guest wasm module and run the host application.
```bash
./run.sh
```

A successful run results in this output:
```
Result: String("foo")
```
