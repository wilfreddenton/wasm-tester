[package]
name = "guest"
version = "0.1.0"
edition = "2024"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { workspace = true }
